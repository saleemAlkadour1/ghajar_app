import 'package:flutter/material.dart';

class AppConfig {
  static const bool defaultHttpLogger = true;
  static const bool httpFullLogEnabled = false;
  static const Duration maxCacheAge = Duration(hours: 48);
  static const String appName = 'Ghajar App';
  static const bool enableTalkerLogger = false;
  static const Size designSize = Size(375, 812);
}
